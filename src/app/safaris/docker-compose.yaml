version: '3.8'
services:
  climocabs:
    image: climocabs
    build:
      context: ./
      # target: dev
      dockerfile: Dockerfile
    # volumes:
    #     - .:/app
    #     - /app/node_modules
    #     - /app/.next
    ports:
      - "3005:3005"